# ПАМЯТЬ ДЖАРВИСА: полное исследование перед кодом
# Все числа ниже — ЗАМЕРЫ в песочнице на настоящем коде проекта.
# Ни одной оценки «на глаз». Где не замерено — написано «НЕ ЗАМЕРЕНО».

## ЧАСТЬ 1. ЧТО УЖЕ ЕСТЬ (главное открытие исследования)

Я искал, чем чинить «замену вместо дубля», и нашёл, что половина
механизма УЖЕ ПОСТРОЕНА и не используется для этой цели.

Таблица memory_fact (core/store.py:205) содержит:
    superseded_by INTEGER REFERENCES memory_fact(id)
    confidence    REAL      (0.0 - 1.0)
    pinned        INTEGER
    source        TEXT      ('explicit' / ...)
    verbatim      TEXT      (слова владельца)
    use_count, last_used_at
  + UNIQUE INDEX на (category,key) WHERE superseded_by IS NULL
    -> «одна ЖИВАЯ строка на ключ, история может повторяться»

Это ровно то устройство, которое нужно для «обновлять»: старая строка не
удаляется, а помечается superseded_by -> новая. История цела, живая одна.
ВЫВОД: изобретать хранилище НЕ НАДО. Надо научиться им пользоваться.

Плюс memory/fact_store.py (685 строк) уже имеет:
  search_facts()  — два индекса FTS5 (unicode61 + trigram), bm25, откат
                    по префиксу для русской морфологии. БЕЗ модели, БЕЗ сети.
  upsert_fact()   — вставка/обновление по (category,key)
  forget_fact()   — удаление
  provenance()    — откуда факт известен

## ЧАСТЬ 2. ЗАМЕР: ГОДИТСЯ ЛИ МЕСТНЫЙ ПОИСК ДЛЯ В3 (замена вместо дубля)

Прогон: 6 настоящих фактов владельца в базу, затем поиск «того же
свойства другими словами».

  MISS  1.39мс  'говори короче'              -> []          (С7)
  MISS  0.46мс  'не надо длинных ответов'    -> []          (С7)
  MISS  0.59мс  'я переехал, теперь в Химках'-> []          (С3)
  OK    0.77мс  'больше не играю в шутеры'   -> game_pref   (С2)
  OK    0.92мс  'теперь стратегии нравятся'  -> game_pref   (С2)
  OK    0.58мс  'не спрашивай каждый раз'    -> act_auton   (С7)
  MISS  0.22мс  'сплю днём'                  -> []  (и должен MISS)

ИТОГ: 3 из 6. Скорость отличная (< 1.5 мс), но ПОЛОВИНУ синонимов не
находит: «говори короче» и «не люблю долгих объяснений» не имеют общих
слов, а FTS ищет слова.

ЧЕСТНЫЙ ВЫВОД: местный поиск НЕ МОЖЕТ быть единственным судьёй В3.
Он годится как ПОДСКАЗКА (дешёвая, быстрая), но не как решение.
Моя вчерашняя надежда «поиск уже есть, значит 1б бесплатно» — ОШИБОЧНА.

## ЧАСТЬ 3. ЗАМЕР: АЛЬТЕРНАТИВА — ПОКАЗАТЬ МОДЕЛИ СПИСОК КЛЮЧЕЙ

Идея: модель сама выберет существующий ключ, если увидит, какие есть.
Цена измеряется в знаках промпта. PROMPT_CHAR_BUDGET = 1200,
у владельца занято 877, свободно 323.

  полный список 15 ключей:            403 знака -> НЕ ВЛЕЗАЕТ (>323)
  список ключей ОДНОЙ категории:       max 77 знаков -> влезает легко

РОСТ (это и есть «продумать на будущее»):
    15 фактов ->   2 ключа/категория ->   34 знака
    50 фактов ->   7 ключей          ->  114 знаков
   100 фактов ->  14 ключей          ->  229 знаков
   300 фактов ->  43 ключа           ->  686 знаков  ПЕРЕПОЛНЕНИЕ
  1000 фактов -> 143 ключа           -> 2286 знаков  КАТАСТРОФА

ВЫВОД: «показать ключи категории» держится ДО ~140 фактов. Дальше нужен
ОТБОР (показывать не все, а найденные поиском). То есть путь 2 и путь 3
не конкуренты — они СОСТАВЛЯЮТСЯ: поиск отбирает кандидатов, модель
выбирает среди них.

## ЧАСТЬ 4. ЗАМЕР: РОСТ БАЗЫ ДО 1000 ФАКТОВ

  поиск 'говори короче': медиана 0.37 мс
  поиск 'факт номер 500': медиана 4.97 мс
  список всех 1000 строк: 8.7 мс
  полный список ключей: 17289 знаков (в 14 раз больше всего бюджета)

ВЫВОД: сама база быстрая и на 1000 фактов. Узкое место НЕ скорость —
узкое место БЮДЖЕТ ПРОМПТА. Это меняет приоритет: беречь надо знаки,
а не миллисекунды.

## ЧАСТЬ 5. АРХИТЕКТУРА (из замеров, а не из вкуса)

Один судья — живая модель (она в разговоре, у неё контекст, цена 0).
Фоновый мозг Б (Memory-Stage1/2 + Personality-Stage1/2, 2-4 вызова НА
КАЖДУЮ реплику) — удалить. Отсечка 90 с исчезает вместе с ним, потому
что платить станет нечем.

Вместо него ТРИ вещи, каждая со своей ценой:

Ш1. КРИТЕРИЙ В ОПИСАНИИ ИНСТРУМЕНТА (цена 0 вызовов, 0 знаков промпта)
    В1 про владельца или про мир; В2 свойство или событие.
    Правка ТЕКСТА описания save_memory. Сегодня там написано
    "Do NOT call for: passing moods" — из-за этой строки «кот лезет на
    клавиатуру» не пишется. Заменить на «событие может обнаружить
    свойство: записывай свойство, не событие».

Ш2. ПОДСКАЗКА ПЕРЕД ЗАПИСЬЮ (цена 0 вызовов, ~77 знаков сегодня)
    Перед сохранением: search_facts(верbatim) отбирает до 3 кандидатов
    из ТОЙ ЖЕ категории, и они показываются модели как
    «похоже, это про то же: preferences/game_preference = prefers shooters».
    Модель либо берёт существующий ключ (-> замена), либо новый.
    Поиск даёт 3 из 6 сам, остальное добирает модель, у которой есть
    контекст. Ни одного лишнего вызова: подсказка едет в тот же ход.
    ПОТОЛОК: не больше N знаков, N жёсткий; при 300+ фактах показываем
    только НАЙДЕННЫХ, а не всю категорию.

Ш3. СВОД В КОНЦЕ РАЗГОВОРА (цена 1 вызов на сессию)
    Видит всю беседу + текущую память. Ловит то, что не поймал ход:
    противоречия (С2/С3), временные свойства (С1), самоопровержения (С8).

## ЧАСТЬ 6. РИСКИ И ЧТО С НИМИ ДЕЛАТЬ

Р1. КОНЕЦ СЕССИИ НЕ НАСТУПАЕТ ЧИСТО. В живом логе владельца 29.08:
    "1006 keepalive ping timeout" — Google рвёт связь сам.
    Если свод (Ш3) висит на «красивом» закрытии, он потеряется именно
    тогда, когда разговор был длинный и интересный.
    РЕШЕНИЕ: свод обязан запускаться по ЛЮБОМУ выходу из сессии, включая
    обрыв, И иметь долговечную отметку «этот разговор не разобран» —
    как журнал двери. Иначе это обещание, а не касса.
    НЕ ЗАМЕРЕНО: доходит ли управление до нужной точки при обрыве 1006.
    Проверить ДО кода.

Р2. main.py ПОД КОНТРОЛЬНОЙ СУММОЙ. Любая правка требует переснять
    эталон голоса (check_lang.py). Владелец принимает это отдельно.

Р3. ГОНКА ПИСАТЕЛЕЙ. В update_memory уже есть шрам: 21.08.2026 факт
    владельца ИСЧЕЗ, когда голосовое «запомни» и фоновый извлекатель
    писали одновременно. Ш2 добавляет ЧТЕНИЕ перед записью — то самое
    окно «прочитал-подумал-записал», из-за которого была потеря.
    РЕШЕНИЕ: чтение кандидатов вне замка (оно только подсказка), но
    сама запись — в одной транзакции, как сейчас. Подсказка устареть
    не страшно: хуже станет только выбор ключа, данные целы.

Р4. ЦЕНА ОШИБКИ ЗАМЕНЫ АСИММЕТРИЧНА.
    Лишний дубль = мусор, лечится позже.
    Ошибочная ЗАМЕНА = ПОТЕРЯ факта владельца, лечится только откатом.
    РЕШЕНИЕ: замена НИКОГДА не удаляет — superseded_by уже есть в схеме.
    При сомнении выбирать дубль, а не замену. Это правило должно быть
    в описании инструмента прямым текстом.

Р5. САМООБМАН ЧАСТОТОЙ (С9). Аллергия, названная раз, важнее хобби,
    названного десять раз. Никакой счётчик повторов не смеет решать.
    РЕШЕНИЕ: счётчик (вчерашняя «достройка 2») ОТЛОЖЕН. Он не нужен для
    В1-В4 и добавляет третьего судью. Вчера я предлагал его слишком рано.

Р6. ЗАПРЕТ ПОМНИТЬ (С10) сильнее всего остального. Сегодня механизма
    нет вовсе. Нужен как отдельное поле/действие, а не как надежда на
    послушание модели.

Р7. ВРЕМЕННЫЕ СВОЙСТВА (С1) требуют поля срока. В схеме его НЕТ.
    Добавление столбца — миграция базы владельца. Отдельный шаг,
    не смешивать с Ш1-Ш3.

## ЧАСТЬ 7. ПОРЯДОК РАБОТ (от дешёвого и обратимого к дорогому)

  Ш1  текст описания          цена 0      обратимо полностью    ПЕРВЫМ
  Ш2  подсказка перед записью цена 0 вызовов, ~77 знаков        ВТОРЫМ
  Ш3  свод в конце            1 вызов/сессия, нужен ответ на Р1 ТРЕТЬИМ
  Ш4  удаление мозга Б        цена -2..-4 вызова/реплика        ПОСЛЕ Ш1-Ш3
  Ш5  С1 (срок) и С10 (запрет) — миграция схемы                 ПОЗЖЕ

Мозг Б удаляется ПОСЛЕДНИМ из первых четырёх нарочно: пока Ш1-Ш3 не
приняты владельцем живьём, старый путь остаётся страховкой. Удалить
раньше — значит остаться без обоих, если Ш1-Ш3 не заработают.

## ЧАСТЬ 8. ЧТО НЕ ЗАМЕРЕНО (долг, а не обещание)
  1. Доходит ли управление до точки свода при обрыве 1006.
  2. Реальная доля попаданий В3 «поиск + модель» — замерить можно только
     живьём на владельце.
  3. Сколько знаков промпта займёт подсказка при 100+ фактах у ЖИВОГО
     владельца (сейчас 13 фактов, прогноз 229 знаков при 100).
  4. Не ломает ли Ш2 существующие 1898 тестов.
