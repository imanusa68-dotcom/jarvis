Verdict: Исследование по показанным направлениям глубокое и опирается на первоисточники: целевая форма ядра (однониточный цикл, единый гейт-диспетчер, реестр действий, шина фактов, SQLite-хранилище, USN/Everything для world model, процессная топология, бюджеты агента) проработана достаточно, чтобы начинать миграцию, а вердикты судей сходятся на жизнеспособном гибриде «C + миграционная механика A + контрактная гигиена B». Самые тонкие места — не архитектура, а операционная обвязка и платформенные факты: (1) судьи делают golden-сценарии, perf-тесты и трейс-консоль merge-гейтами каждого этапа, но ни одно направление не исследовало, КАК тестировать и наблюдать недетерминированную агентную систему — эти гейты сейчас декларации без методологии; (2) три судьи дали три несовместимых рецепта шифрования данных в покое и все трое отметили нерешённую elevation-проблему USN/MFT — блокирующие решения этапов 1–2 приняты на противоречивых предположениях; (3) жизненный цикл самой системы (установка, автозапуск, self-update с откатом, миграции схем нескольких SQLite-файлов, бэкап живой WAL-базы) не исследован вовсе, хотя горизонт — 5+ лет, и судьи уже поймали в этой зоне фактическую ошибку (бэкап WAL простым копированием). Оговорка: дайджест показывает 8 из 11 направлений — если непоказанные покрывают память/голосовой каскад/UI, соответствующие пробелы ниже не дублировать; независимо от этого четыре выбранных пробела не покрыты ни дайджестом, ни вердиктами.

## Gaps

- **Стратегия тестирования агентной голосовой системы (golden-сценарии, LLM-replay, crash-injection)**: Все три судьи назначают golden-сценарии смоук-гейтом каждого этапа 8-этапной миграции и требуют «журнал расхождений» старого/нового цикла, но ни одно направление исследования не отвечает, как воспроизводимо тестировать систему с недетерминированной LLM в центре; без методологии главная страховочная сетка миграции не существует, и регрессии гейта разрешений или undo-журнала обнаружит пользователь на своих файлах. | Задание: Контекст: single-user desktop-ассистент на Python/asyncio под Windows 11; ядро — while-цикл с tool calls, единый permission-гейт (deny/confirm/auto), saga-журнал файловых операций с undo/staging, голосовой контур (realtime-аудио Gemini Live за интерфейсом VoiceSession), план миграции из 8 этапов с golden-сценариями как merge-гейтом. Исследовать по первоисточникам и реальным кодовым базам: (1) record/replay LLM-взаимодействий для регрессионных тестов — VCR-подобные кассеты (pytest-recording, vcrpy), детерминированные фейк-модели со scripted tool-call последовательностями; как тестируют свои агентные циклы Claude Code/Agent SDK, aider, Open Interpreter, gptme (у aider и gptme открытые тестовые наборы — разобрать конкретно); (2) применимость eval-фреймворков (promptfoo, DeepEval, pytest-evals, Braintrust) для соло-разработчика с сотнями запросов в день — что даёт ценность, что церемония; (3) тестирование инфраструктуры БЕЗ LLM: тест «хендлер инструмента недоступен вне dispatch()», import-linter/grep-правила как CI-гейт (запрет os/shutil/subprocess в tools/ и plugins/), crash-injection для saga-журнала (kill процесса между intent-записью и completion — проверка докатки/отката и идемпотентности шагов), property-based тесты (hypothesis) для ScopedRoot и планов; (4) тестирование голосового контура без реального API: фейковый VoiceSession-драйвер, подача записанных событийных трейсов, тесты interruption/steering-кейсов (судья data-safety требует их в golden-наборе); (5) perf-бюджет как тестовый артефакт: как реальные проекты меряют в CI idle RSS и число пробуждений процесса; (6) практика CI на Windows-раннерах (GitHub Actions) для кода с pywin32/COM/asyncio. Результат: рекомендуемая пирамида тестов с конкретными библиотеками, примерами из названных проектов и шаблоном golden-сценария (формат записи, критерий прохождения, обновление при смене модели).
- **Наблюдаемость и отладка агентного поведения локально (трейсинг, replay сессий, метрики)**: Судьи требуют «отладочную консоль-подписчик шины», correlation ID и метрики confirm-rate/undo-rate/эскалаций как управляющий контур, но исследование нигде не описывает, как соло-разработчику дёшево собрать трейс «голос→план→гейт→действие→undo» и воспроизводить сессии; без этого каждая аномалия за годы эксплуатации — археология логов, а «управляемая» шкала риска остаётся без данных для управления. | Задание: Контекст: локальный Python/asyncio-демон с event bus (~15 типов событий), LLM-циклом, permission-гейтом и SQLite-хранилищем; облачные SaaS-наблюдаемости нежелательны (личные данные), бюджет ресурсов жёсткий (idle ≤150 МБ RSS). Исследовать: (1) OpenTelemetry GenAI semantic conventions (gen_ai.*) — текущий статус стабильности, применимость к собственному agent loop без фреймворка, минимальная инструментация; (2) self-hosted инструменты трейсинга LLM (Langfuse self-hosted, Arize Phoenix, Logfire, OpenLLMetry) — реальный ресурсный footprint каждого, и главный вопрос: жизнеспособен ли вариант «трейсы просто в свою SQLite + просмотрщик» вместо отдельного сервера — есть ли прецеденты (как Claude Code хранит append-only сессии и что позволяет их replay); (3) структурное логирование агента: сквозной correlation ID через цепочку голос→план→шаг→гейт→действие→журнал, полные промпты/ответы с ротацией и ретенцией (это чувствительные данные — состыковать с политикой хранилища истории), учёт токенов/стоимости на запрос и дневные бюджеты; (4) session replay для отладки: что минимально нужно сохранить, чтобы реконструировать «почему агент так решил» (входной контекст, схемы тулов, seed/параметры, версии промптов), как это делают LangSmith/AutoGen (OpenTelemetry) и Claude Code (--resume); (5) «flight recorder» шины: кольцевой буфер последних N событий, консоль-подписчик, генерация EVENTS.md из каталога типизированных событий — примеры реализации (Home Assistant dev tools как образец); (6) какие метрики агентного поведения осмысленны при single-user (confirm-rate, undo-rate, доля эскалаций роутера, стагнация/повторные вызовы, глубина очередей подписчиков) и как их писать в ту же SQLite без отдельного стека. Результат: рекомендованный минимальный стек наблюдаемости с оценкой цены в RSS/строках кода и списком «не брать» (тяжёлые серверные стеки).
- **Windows-платформенные развязки: elevation для USN/MFT, шифрование данных в покое, условия использования Everything**: Это прямое противоречие между судьями, требующее фактов: три разных рецепта шифрования (SQLCipher+DPAPI-ключ vs EFS vs BitLocker+ACL vs осознанный отказ), общее для всех замечание «чтение \\.\C: требует прав администратора — ни один вариант этого не заметил», и расхождение по механизму IPC Everything (WM_COPYDATA vs named pipe); от ответов зависят решения этапов 1–2 (владелец file-ingest, схема хранилища history.db, основной путь файлового индекса). | Задание: Контекст: ассистент индексирует файлы через NTFS MFT/USN Journal (или делегирует Everything), хранит самые чувствительные данные (история активности, память) в SQLite (WAL, FTS5, sqlite-vec) на Windows 11 Pro; ассистент не должен целиком работать под администратором. Закрыть три блока фактами из первоисточников (Microsoft Learn, voidtools, SQLCipher/Zetetic): (1) Elevation: какие именно привилегии нужны для FSCTL_ENUM_USN_DATA, FSCTL_READ_USN_JOURNAL и открытия тома (Administrators? SeManageVolumePrivilege? Backup Operators?); есть ли непривилегированные альтернативы (FSCTL_READ_UNPRIVILEGED_USN_JOURNAL — его ограничения и достаточность для инкрементов); как устроена служба Everything (что делает Everything Service, как клиент 1.4/1.5 общается с ней, работает ли индексация без службы); паттерны мини-службы-хелпера на Python/pywin32 с узким IPC-API и её установка/обновление; (2) Encryption-at-rest для живых SQLite-БД: SQLCipher — лицензия, активность проекта, состояние Python-биндингов (sqlcipher3-wheels, apsw-sqlite3mc, SQLite3MultipleCiphers), совместимость с WAL/FTS5/загружаемыми расширениями (sqlite-vec) и измеренные накладные расходы; EFS — доступность по редакциям Windows 11 (Home vs Pro), поведение с WAL/shm-файлами; BitLocker/Device Encryption — что включено по умолчанию на Windows 11 и от каких сценариев реально защищает на однопользовательском ПК; DPAPI/DPAPI-NG для обёртки ключей и секретов; итог оформить матрицей «модель угрозы (кража диска / другой пользователь / вредоносный процесс в сессии) → работающий механизм → цена»; (3) Everything: точные условия лицензии на использование SDK/ES.exe из стороннего приложения и редистрибуцию, различия и стабильность IPC в 1.4 (WM_COPYDATA SDK, ES.exe) против 1.5 alpha (named pipe \\.\PIPE\Everything IPC), поведение при неустановленной/остановленной службе, прецеденты интеграции сторонних инструментов. Результат: по каждому блоку — конкретное решение для проекта с цитатами и ссылками.
- **Жизненный цикл самой системы: установка, автозапуск, self-update с откатом, миграции схем SQLite, бэкап/восстановление**: Архитектура рассчитана на 5+ лет с несколькими SQLite-файлами, эволюционирующим каталогом событий и append-only журналами, но ни одно направление не исследовало, как систему обновлять и мигрировать данные без потери; судьи уже поймали ошибку бэкапа WAL-базы простым копированием — индикатор, что вся зона «данные переживают обновления и откаты кода» держится на неявных предположениях. | Задание: Контекст: соло-мейнтейнер, always-on Python-демон на Windows 11, 3–4 процесса, данные в jarvis.db (вечное) + history.db (вытесняемое) в WAL-режиме, plus saga-журнал операций; система должна обновляться годами без потери данных и уметь откатываться на предыдущую версию. Исследовать: (1) упаковка и запуск: uv с lock-файлом + управляемый Python против PyInstaller/Briefcase для фоновых сервисов (урок Leon о компиляции рантаймов); автозапуск демона (Task Scheduler vs startup vs Windows-служба) с graceful shutdown/restart и single-instance гарантией; (2) self-update: паттерн «обновиться, прогнать health-check (golden-смоук), при провале автоматически откатиться на предыдущую версию» — как реализуют Tauri updater, WinSparkle/Squirrel, Home Assistant (Supervisor + слоты версий), OVOS; двухслотовая схема A/B для приложения на Python; совместное версионирование кода и данных (код версии N-1 против БД, мигрированной версией N — политика downgrade); (3) миграции схем SQLite для нескольких файлов: PRAGMA user_version + линейные скрипты против Alembic/каркасов (caribou, yoyo, golembic) — что адекватно соло-проекту; правила эволюции append-only журналов и типизированных событий шины (строгая аддитивность, api_version — как формализовать проверку в CI); опыт долгоживущих локальных приложений (Home Assistant recorder migrations, Firefox places.sqlite, Syncthing) по обратной совместимости; (4) бэкап живых WAL-баз: VACUUM INTO vs sqlite3 backup API vs Litestream/litestream-подобная непрерывная репликация — цена и пригодность для десктопа; консистентность НЕСКОЛЬКИХ файлов БД между собой на момент бэкапа (нужен ли общий снапшот или допустима слабая связность jarvis.db/history.db); регулярный тест восстановления как часть CI/health-check; (5) быстрый лицензионный аудит ключевых зависимостей целевого стека (pluggy, watchdog, pywin32/comtypes, fastembed + ONNX Runtime, sqlite-vec, faster-whisper/CTranslate2, Piper TTS, MCP SDK) — лицензии, совместимость, риски для многолетнего личного проекта. Результат: рекомендованные процедуры install/update/rollback/backup/restore с конкретными инструментами и ссылками на первоисточники.