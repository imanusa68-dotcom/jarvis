# -*- coding: utf-8 -*-
"""Пакет core. ЭТОТ ФАЙЛ ОБЯЗАТЕЛЕН, НЕ УДАЛЯТЬ.

Без него папка работает как namespace-пакет, а такой пакет ПРОИГРЫВАЕТ
обычному файлу с тем же именем в любом месте пути поиска. Одноимённый
файл в чужом проекте молча подменяет наш код целиком.

НАЙДЕНО НА МАШИНЕ ВЛАДЕЛЬЦА 28.08.2026, А НЕ В ТЕОРИИ. Прогон упал с
семью ошибками сбора, и в тексте одной из них стоял чужой путь:
    cannot import name 'contracts' from 'agent'
    (C:/Users/rdrr/Downloads/browser-use-perf-final/browser-use-main/agent.py)
То есть `import agent` привёл в СОСЕДНИЙ проект в Downloads. На Linux в
песочнице этого видно не было: там рядом нет чужого agent.py, и папка
притворялась пакетом успешно. Целый класс поломок был невидим.

Замерено опытом, а не выведено рассуждением: подмена происходит и когда
чужая папка стоит в пути ПОСЛЕ корня проекта. Значит порядок путей
(pythonpath в pyproject.toml) от неё не спасает — спасает только
настоящий пакет, то есть этот файл.

Файл ПУСТОЙ по содержанию нарочно. Импорты здесь исполнялись бы при
каждом обращении к любому модулю пакета, и это стоило бы времени на
старте — того самого, за которое воюем в бюджете быстрых путей.
"""
